export interface MetaFields {
  title: string;
  description: string;
  image_url: string;
  url: string;
}

export interface ActiveFields {
  description: boolean;
  image_url: boolean;
  url: boolean;
}
